function handleEscClose(event) {
  // PASO 1: Detectar si la tecla presionada es "Escape"
  // - Comprobar si event.key === "Escape"
  //
  // PASO 2: Buscar si existe algún popup actualmente abierto
  // - Usar document.querySelector(".popup_opened") para obtenerlo
  //
  // PASO 3: Cerrar el popup abierto (si existe)
  // - Llamar a closePopup(openedPopup)
};
