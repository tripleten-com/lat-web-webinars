// PASO 1: Importar la funcion hasInvalidInput

export function toggleButtonState(inputList, buttonElement) {
  // PASO 2: Verificar si existe algún input inválido en el formulario
  // - Llamar a la función hasInvalidInput(inputList)
  //
  // PASO 3: Cambiar el estado del botón según la validez de los inputs
  // - Si hay al menos un campo inválido → deshabilitar el botón (disabled = true)
  // - Si todos los campos son válidos → habilitar el botón (disabled = false)
};
